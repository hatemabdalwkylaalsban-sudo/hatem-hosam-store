# 🔐 تقرير الأمان - متجر حاتم وحسام

## مقدمة

هذا التقرير يوثق الإجراءات الأمنية المطبقة في المشروع لحماية:
- بيانات المستخدمين
- قاعدة البيانات
- جلسات المستخدمين
- الصلاحيات

## 🛡️ 1. حماية كلمات المرور

### المشكلة
تخزين كلمات المرور كنص صريح (plain text) يعني أنه عند اختراق قاعدة البيانات، يستطيع المهاجم رؤية كل كلمات المرور واستخدامها.

### الحل المطبق

عند التسجيل:
`php
$hash = password_hash($password, PASSWORD_DEFAULT);